cmake_minimum_required(VERSION 3.5...3.27)

# ============================================================================
# MATLAB Coder Generated Library
# ============================================================================
# This library contains code auto-generated by MATLAB Coder
# License: Academic License (MATLAB)
# Original Author: MathWorks, Inc.
#
# Files generated from MATLAB implementations of:
# - Phase Congruency (Peter Kovesi)
# - Log-Gabor filters
# - Various mathematical utility functions
#
# See README.md for detailed information about sources and licensing.
# ============================================================================

file(GLOB MATLAB_CODER_SOURCES "src/*.cpp")
file(GLOB MATLAB_CODER_HEADERS "include/*.h" "include/*.hpp")

add_library(contrib_matlab_coder ${MATLAB_CODER_SOURCES} ${MATLAB_CODER_HEADERS})

target_include_directories(contrib_matlab_coder
    PUBLIC
        ${CMAKE_CURRENT_SOURCE_DIR}/include
)

target_link_libraries(contrib_matlab_coder
    PUBLIC
        le::opencv
)

# Disable warnings for auto-generated code
target_compile_options(contrib_matlab_coder PRIVATE
    -Wno-old-style-cast
    -Wno-conversion
    -Wno-sign-conversion
    -Wno-effc++
    -Wno-unused-parameter
    -Wno-unused-variable
)
