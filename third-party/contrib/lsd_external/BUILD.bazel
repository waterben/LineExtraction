"""External LSD implementations - AGPL and other licenses"""

load("@rules_cc//cc:defs.bzl", "cc_library")

package(default_visibility = ["//visibility:public"])

cc_library(
    name = "lsd_external",
    srcs = glob(
        ["src/*.cpp"],
        allow_empty = True,
    ),
    hdrs = glob(
        [
            "include/*.h",
            "include/*.hpp",
        ],
        allow_empty = True,
    ),
    copts = [
        "-Wno-old-style-cast",
        "-Wno-conversion",
        "-Wno-sign-conversion",
        "-Wno-effc++",
        "-Wno-unused-parameter",
        "-Wno-unused-variable",
        "-Wno-sign-compare",
    ],
    includes = ["include"],
    visibility = ["//visibility:public"],
    deps = [
        "@opencv",
    ],
)
